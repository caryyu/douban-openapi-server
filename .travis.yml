language: minimal

services:
  - docker

before_install:
  - docker build -t caryyu/douban-openapi-server:$TRAVIS_COMMIT .

deploy:
  provider: script
  script: bash scripts/docker_push
  on:
    branch: main

